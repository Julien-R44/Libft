#include "../Libft/libft.h"
#include <stdio.h>

void	ft_realloc(void *ptr, size_t size)
{
	void	*danew;

	danew = ft_memalloc()
}

int main(void)
{
	char *str;

	str = ft_memalloc(sizeof(char) * 10);
	printf("%d\n", sizeof(str));
	return (0);
}